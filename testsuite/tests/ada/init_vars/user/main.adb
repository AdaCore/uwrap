with Pck_Wrapped; use Pck_Wrapped;
with Ada.Text_IO; use Ada.Text_IO;

procedure Main is
begin
   Put_Line (Some_Var'Img);
   Put_Line (Some_Const'Img);
   Put_Line (Some_Other_Var);
   Put_Line (Some_Other_Const);   
end Main;

