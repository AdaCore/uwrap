template w_Entity {
    var name : text;
}

match Entity (".(?<a>.+)")
wrap w_Entity (name => a);

match Entity ("^A$")
wrap standard.out ("FIND:" & lambda (w_Entity.find (v: true).fold (l: "", l: (l & " [" & v.name & "]")) & "\n"));
